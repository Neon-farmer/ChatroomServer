# Instructions for model organization and use #

One class per file.

Naming convention - (SQL server table name) + Model.

Model files contain classes representing the objects in the database, but not the methods for modifying them.

